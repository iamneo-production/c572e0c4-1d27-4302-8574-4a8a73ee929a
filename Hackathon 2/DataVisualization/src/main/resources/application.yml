
server:
  port: 8094
  
spring:
  application:
    name:  DATAVISUALIZE-SERVICE
  datasource:
    username: root
    password: admin
    url: jdbc:mysql://localhost:3306/hack2
  jpa:
    hibernate:
      ddl-auto: update

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/

  instance:
    prefer-ip-address: true
    
